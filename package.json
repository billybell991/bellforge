{
  "name": "bellforge",
  "version": "1.0.0",
  "private": true,
  "description": "BellForge — Forge your own Android game, no code required.",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "render-build": "cd server && npm install && npm run build && cd ../client && npm install && npx vite build",
    "render-start": "cd server && node dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
